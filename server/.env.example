# Example development environment
NODE_ENV=development
PORT=3009

# Database (replace with real credentials)
DATABASE_URL=postgresql://postgres:password@localhost:5432/mangu_dev

# JWT secret for local dev (32+ characters)
JWT_SECRET=development-jwt-secret-key-at-least-32-characters-long

# Disable Redis during dev
DISABLE_REDIS=1

# Frontend URL for CORS
CLIENT_URL=http://localhost:5179
