name: CodeQL

on:
  push: { branches: ["**"] }
  pull_request:
  schedule: [{ cron: "0 2 * * *" }]

jobs:
  analyze:
    uses: github/codeql-action/.github/workflows/codeql.yml@v3
    with:
      languages: javascript, python
